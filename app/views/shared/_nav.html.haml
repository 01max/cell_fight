- fighters_class = %w(fighters).include?(params['controller']) ? 'active' : ''

%ul.nav.navbar-nav
  %li.nav-item
    = link_to fighters_path, { class: "nav-link #{fighters_class}" } do
      %i.fas.fa-vial
      = Fighter.model_name.human.pluralize.humanize
