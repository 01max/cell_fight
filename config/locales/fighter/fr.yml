
fr:
  activerecord:
    models:
      fighter:
        zero: combattant
        one: combattant
        other: combattants

    attributes:
      fighter:
        id: id
        name: nom

        attack_base_points: points de base d'attaque
        defense_base_points: points de base de défense
        health_base_points: points de base de santé

        total_base_points: points de base

    errors:
      models:
        fighter:
          attack_base_points:
            total_base_points_must_be_below: "Le total des points de base distribués doit être inférieur à %{max_value}"
